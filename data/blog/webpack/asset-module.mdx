---
title: webpack 资源模块的概念
date: '2023-03-25'
tags: ['webpack']
draft: true
summary: webpack 是一个现代 JavaScript 应用程序的静态模块打包工具。当 webpack 处理应用程序时，它会将一切资源都看作是模块，不管是代码资源如 JS、CSS 等，还是非代码资源如图片、字体等，这些模块之间是会存在依赖关系的。从入口开始，它会递归地构建一个依赖关系图，其中包含应用程序所需的每个模块，然后将这些模块打包成一个或多个bundle。在 webpack 中，entry、output、loader、plugins 和 mode 等配置项决定了如何构建依赖关系图。
---

资源模块是 webpack 5 新增的一种概念，具体表现是对图片、字体等资源的解析方式发生了变化，可以不再使用 loader 处理这些资源模块，取而代之的是使用一个 type 字段配置即可。

webpack 5 实现了 4 种新的模块类型，通过 Rule 的 type 属性设置：

- asset/resource: 输出一个单独的文件并导出 URL —— 取代 file-loader
- asset/inline: 导出一个资源的 data URI —— 取代 url-loader
- asset/source: 导出资源的源代码 —— 取代 raw-loader
- asset: 在导出资源的 data URI 和输出一个单独的文件之间自动选择 —— 取代 url-loader，之前是在 url-loader 中通过配置资源体积限制实现

> 官方文档 [Asset Modules](https://webpack.js.org/guides/asset-modules/)

# 资源模块类型的解析

## asset/resouse

## asset/inline

## asset/source

## asset

# 修改资源模块的输出文件名与路径
